{
  "$schema": "https://json-structure.org/meta/core/v0/#",
  "$id": "https://raw.githubusercontent.com/api-evangelist/webhooks/refs/heads/main/json-structure/webhooks-delivery-attempt-structure.json",
  "name": "DeliveryAttempt",
  "description": "Represents a single attempt to deliver a webhook event to a consumer endpoint, including request, response, and outcome metadata.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Identifier for this delivery attempt.",
      "example": "att_01HXYZ4P9V"
    },
    "event_id": {
      "type": "string",
      "description": "Identifier of the source event being delivered.",
      "example": "evt_3NqA8c2eZvKYlo2C"
    },
    "endpoint_id": {
      "type": "string",
      "description": "Identifier of the destination webhook endpoint.",
      "example": "we_1NqA8c2eZvKYlo2C"
    },
    "attempt_number": {
      "type": "int32",
      "description": "1-based attempt number for the underlying event delivery.",
      "example": 3
    },
    "status": {
      "type": "string",
      "enum": ["succeeded", "failed", "pending", "scheduled", "skipped"],
      "description": "Outcome of this delivery attempt.",
      "example": "failed"
    },
    "request": {
      "type": "object",
      "description": "Outbound HTTP request as sent to the consumer endpoint.",
      "properties": {
        "method": { "type": "string", "enum": ["POST", "PUT"], "example": "POST" },
        "url": { "type": "uri", "example": "https://api.example.com/webhooks/stripe" },
        "headers": {
          "type": "map",
          "values": { "type": "string" }
        },
        "body_size_bytes": { "type": "int32", "example": 4823 }
      }
    },
    "response": {
      "type": "object",
      "description": "Response received from the consumer endpoint, if any.",
      "properties": {
        "status_code": { "type": "int32", "example": 503 },
        "headers": {
          "type": "map",
          "values": { "type": "string" }
        },
        "body_excerpt": {
          "type": "string",
          "description": "Truncated response body for debugging.",
          "example": "Service Unavailable"
        },
        "latency_ms": { "type": "int32", "example": 312 }
      }
    },
    "error": {
      "type": "object",
      "description": "Error details when the attempt did not succeed.",
      "properties": {
        "code": {
          "type": "string",
          "enum": ["timeout", "connection_refused", "dns_failure", "tls_error", "non_2xx_response", "payload_too_large", "rate_limited"],
          "example": "non_2xx_response"
        },
        "message": { "type": "string", "example": "Endpoint returned 503 after 312ms" }
      }
    },
    "next_retry_at": {
      "type": "datetime",
      "description": "Scheduled time of the next retry attempt, if any.",
      "example": "2026-05-19T15:09:05Z"
    },
    "attempted_at": {
      "type": "datetime",
      "example": "2026-05-19T15:04:05Z"
    }
  },
  "required": ["event_id", "endpoint_id", "attempt_number", "status", "attempted_at"]
}
